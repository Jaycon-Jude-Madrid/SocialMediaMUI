import { createTheme } from "@mui/system";

export const theme = createTheme({
  palette: {
    primay: {
      main: "#5ED2D5",
      light: "#ff0000",
    },
    secondary: {
      main: "#ff0000",
    },
    otherColor: {
      main: "#ff0000",
    },
  },
});
